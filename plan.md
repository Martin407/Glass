1. Extract SSE stream processing into `stream.ts`.
2. Update `index.ts` to import `processSessionStream` and use it inside the route handler.
3. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
4. Submit PR.
